services:
  - type: web
    name: imposter-game
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production

  # Redis service removed — using in-memory room store instead
